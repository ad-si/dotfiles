#! /usr/bin/env bash

packages=(
  eslint
  forever
  live-server
  markdown-link-check
  msee
  nodemon
  npm-check-updates
  qrcode
  rethinkdb
  serverless
  supervisor
  surge
  tldr
  trash
)

for package in "${packages[@]}"
do
  npm install --global "$package"
done
