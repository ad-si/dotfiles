#!/bin/dash

set -euf

topgrade \
  --no-retry \
  --disable bun \
  --disable cargo \
  --disable containers \
  --disable go \
  --disable nix \
  --disable node \
  --disable pnpm \
  --disable uv

NIXPKGS_ALLOW_UNFREE=1 nix profile upgrade --impure nix-config
