#! /bin/bash

counter=1
numberOfFiles=$(printf "%d" $(ls | wc -l))
numberOfDigits="${#numberOfFiles}"

for file in $(ls)
do
  mv "$file" "$(printf %0${numberOfDigits}d $counter).${file##*.}"
  counter=$((counter + 1))
done
