#! /usr/bin/env bash

counter=1

#for a in $(ls)
#do
# mv $a $counter.png
# counter=$((counter + 1))
#done

echo 'TODO: use `find -exec` instead of a for loop'

# for a in $(find . -name '*.png')
# do
#   magick convert "$a" -verbose -trim "$a"
# done
